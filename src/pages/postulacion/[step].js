import React from "react";
import { NextPage } from "next";
import { StateMachine } from "../../components/StateMachine";

const Home = () => {
  return (
    <div>
      <StateMachine />
    </div>
  );
};

export default Home;
